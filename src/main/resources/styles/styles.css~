/* styles.css */

/* Root styling */
.root {
    -fx-font-family: "Arial";
    -fx-background-color: #ffffff; /* Default background for the entire application */
}

/* Left Pane Styling */
.left-pane {
    -fx-background-color: #06234f/* Blue gradient */
    -fx-alignment: center;
}
/* General Styling */
.root {
    -fx-font-family: 'Arial';
    -fx-background-color: #F8F9FA;
}

/* Labels */
.title-label {
    -fx-font-size: 20px;
    -fx-font-weight: bold;
    -fx-text-fill: #333;
}

.subtitle-label {
    -fx-font-size: 14px;
    -fx-text-fill: #555;
}

.section-label {
    -fx-font-size: 16px;
    -fx-font-weight: bold;
    -fx-text-fill: #007BFF;
}

/* Text Fields */
.text-field {
    -fx-pref-width: 200px;
    -fx-padding: 5px;
    -fx-border-color: #CCC;
    -fx-border-radius: 5px;
    -fx-background-radius: 5px;
}

.text-field:focused {
    -fx-border-color: #007BFF;
}

/* Buttons */
.button {
    -fx-padding: 8px 15px;
    -fx-font-size: 14px;
    -fx-background-color: #007BFF;
    -fx-text-fill: white;
    -fx-border-radius: 5px;
    -fx-background-radius: 5px;
}

.button:hover {
    -fx-background-color: #0056b3;
}

.cancel-button {
    -fx-background-color: #6C757D;
}

.cancel-button:hover {
    -fx-background-color: #5A6268;
}

.image-button {
    -fx-background-color: transparent;
    -fx-text-fill: #007BFF;
    -fx-underline: true;
}

.image-button:hover {
    -fx-text-fill: #0056b3;
}

/* Layout */
VBox {
    -fx-background-color: white;
    -fx-padding: 20px;
    -fx-border-color: #DDD;
    -fx-border-radius: 10px;
    -fx-background-radius: 10px;
}
#userIcon {
    -fx-background-radius: 50%;
    -fx-border-radius: 50%;
    -fx-border-color: white;
}

HBox {
    -fx-padding: 10px 0;
}

/* App Name Label */
.app-name-label {
    -fx-font-size: 32px;
    -fx-font-weight: bold;
    -fx-text-fill: white;
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 5, 0.5, 0, 1); /* Subtle shadow for depth */
}
/* Sign-Up Title Label */
.signup-title-label {
    -fx-font-size: 28px;
    -fx-font-weight: bold;
    -fx-text-fill: #333333; /* Dark gray for contrast */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 2, 0.5, 0, 1); /* Subtle shadow */
}

/* Sign-Up Button */
.signup-button {
    -fx-background-color: linear-gradient(to bottom right, #1e3c72, #2a5298); /* Gradient background */
    -fx-text-fill: white; /* White text */
    -fx-font-size: 16px;
    -fx-padding: 12px 30px; /* Padding for button */
    -fx-border-radius: 5px; /* Rounded corners */
    -fx-cursor: hand; /* Hand cursor on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0.5, 0, 2); /* Subtle shadow */
}

/* Verification Title Label */
.verify-title-label {
    -fx-font-size: 24px;
    -fx-font-weight: bold;
    -fx-text-fill: #333333; /* Dark gray for contrast */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 2, 0.5, 0, 1); /* Subtle shadow */
}

/* Verification Button */
.verify-button {
    -fx-background-color: #4a90e2; /* Blue background */
    -fx-text-fill: white; /* White text */
    -fx-font-size: 16px;
    -fx-padding: 12px 30px; /* Padding for button */
    -fx-border-radius: 5px; /* Rounded corners */
    -fx-cursor: hand; /* Hand cursor on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0.5, 0, 2); /* Subtle shadow */
}

.verify-button:hover {
    -fx-background-color: #357ABD; /* Darker blue on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 10, 0.5, 0, 5); /* Larger shadow on hover */
}
/* Reset Password Title Label */
.reset-title-label {
    -fx-font-size: 28px;
    -fx-font-weight: bold;
    -fx-text-fill: #333333; /* Dark gray for contrast */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 2, 0.5, 0, 1); /* Subtle shadow */
}

/* Reset Button */
.reset-button {
    -fx-background-color: linear-gradient(to bottom right, #1e3c72, #2a5298); /* Gradient background */
    -fx-text-fill: white; /* White text */
    -fx-font-size: 16px;
    -fx-padding: 12px 30px; /* Padding for button */
    -fx-border-radius: 5px; /* Rounded corners */
    -fx-cursor: hand; /* Hand cursor on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0.5, 0, 2); /* Subtle shadow */
}

.reset-button:hover {
    -fx-background-color: linear-gradient(to bottom right, #2a5298, #1e3c72); /* Reverse gradient on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 10, 0.5, 0, 5); /* Larger shadow on hover */
}

/* Additional Styling for the Right Pane */
.right-pane {
    -fx-background-color: #ffffff; /* White background */
    -fx-alignment: center;
    -fx-padding: 40;
    -fx-spacing: 20; /* Consistent spacing between elements */
}

/* Additional Styling for the Left Pane */
.left-pane {
    -fx-background-color: #06234f; /* Blue gradient */
    -fx-alignment: center;
    -fx-spacing: 20; /* Consistent spacing between elements */
}

/* Additional Styling for the App Logo */
ImageView {
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 10, 0.5, 0, 5); /* Subtle shadow for depth */
}

/* Additional Styling for the App Description */
.app-description-label {
    -fx-font-size: 18px;
    -fx-text-fill: rgba(255, 255, 255, 0.8); /* Semi-transparent white */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0.5, 0, 2); /* Subtle shadow */
}

/* Additional Styling for the Message Label */
.message-label {
    -fx-font-size: 14px;
    -fx-text-fill: #ff0000; /* Red for error messages */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 2, 0.5, 0, 1); /* Subtle shadow */
}

/* Additional Styling for the Email Field */
.email-field {
    -fx-background-color: #f5f5f5; /* Light gray background */
    -fx-border-color: #cccccc; /* Light gray border */
    -fx-border-width: 1px;
    -fx-border-radius: 5px; /* Rounded corners */
    -fx-padding: 10px;
    -fx-pref-width: 250px; /* Medium size */
    -fx-font-size: 14px;
    -fx-text-fill: #333333; /* Dark gray text */
    -fx-prompt-text-fill: #999999; /* Light gray placeholder text */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 5, 0.5, 0, 2); /* Subtle shadow */
}

.email-field:focused {
    -fx-border-color: #4a90e2; /* Blue border when focused */
    -fx-effect: dropshadow(gaussian, rgba(74, 144, 226, 0.3), 10, 0.5, 0, 5); /* Glow effect */
}
/* Root styling */
.root {
    -fx-font-family: "Arial";
    -fx-background-color: #ffffff; /* Default background for the entire application */
}

/* Left Pane Styling */
.left-pane {
    -fx-background-color: linear-gradient(to bottom right, #1e3c72, #2a5298); /* Blue gradient */
    -fx-alignment: center;
}

/* App Name Label */
.app-name-label {
    -fx-font-size: 32px;
    -fx-font-weight: bold;
    -fx-text-fill: white;
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 5, 0.5, 0, 1); /* Subtle shadow for depth */
}

/* App Description Label */
.app-description-label {
    -fx-font-size: 18px;
    -fx-text-fill: rgba(255, 255, 255, 0.8); /* Semi-transparent white */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0.5, 0, 2); /* Subtle shadow */
}

/* Right Pane Styling */
.right-pane {
    -fx-background-color: #ffffff; /* White background */
    -fx-alignment: center;
    -fx-padding: 40;
    -fx-spacing: 20; /* Consistent spacing between elements */
}

/* Reset Password Title Label */
.reset-title-label {
    -fx-font-size: 28px;
    -fx-font-weight: bold;
    -fx-text-fill: #333333; /* Dark gray for contrast */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 2, 0.5, 0, 1); /* Subtle shadow */
}

/* Input Fields (New Password and Confirm Password) */
.input-field {
    -fx-background-color: #f5f5f5; /* Light gray background */
    -fx-border-color: #cccccc; /* Light gray border */
    -fx-border-width: 1px;
    -fx-border-radius: 5px; /* Rounded corners */
    -fx-padding: 10px;
    -fx-pref-width: 250px; /* Medium size */
    -fx-font-size: 14px;
    -fx-text-fill: #333333; /* Dark gray text */
    -fx-prompt-text-fill: #999999; /* Light gray placeholder text */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 5, 0.5, 0, 2); /* Subtle shadow */
}

.input-field:focused {
    -fx-border-color: #4a90e2; /* Blue border when focused */
    -fx-effect: dropshadow(gaussian, rgba(74, 144, 226, 0.3), 10, 0.5, 0, 5); /* Glow effect */
}

/* Reset Button */
.reset-button {
    -fx-background-color: linear-gradient(to bottom right, #1e3c72, #2a5298); /* Gradient background */
    -fx-text-fill: white; /* White text */
    -fx-font-size: 16px;
    -fx-padding: 12px 30px; /* Padding for button */
    -fx-border-radius: 5px; /* Rounded corners */
    -fx-cursor: hand; /* Hand cursor on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0.5, 0, 2); /* Subtle shadow */
}

.reset-button:hover {
    -fx-background-color: linear-gradient(to bottom right, #2a5298, #1e3c72); /* Reverse gradient on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 10, 0.5, 0, 5); /* Larger shadow on hover */
}

/* Message Label */
.message-label {
    -fx-font-size: 14px;
    -fx-text-fill: #ff0000; /* Red for error messages */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 2, 0.5, 0, 1); /* Subtle shadow */
}

/* Message Label */
.message-label {
    -fx-font-size: 14px;
    -fx-text-fill: #ff0000; /* Red for error messages */
}
.signup-button:hover {
    -fx-background-color: linear-gradient(to bottom right, #2a5298, #1e3c72); /* Reverse gradient on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 10, 0.5, 0, 5); /* Larger shadow on hover */
}
/* App Description Label */
.app-description-label {
    -fx-font-size: 18px;
    -fx-text-fill: rgba(255, 255, 255, 0.8); /* Semi-transparent white */
}
/* General Styles */
.root {
    -fx-font-family: 'Segoe UI', sans-serif;
    -fx-font-size: 14px;
}

/* Header Styles */
.header-label {
    -fx-text-fill: #FFFFFF;
    -fx-font-size: 36px;
    -fx-font-weight: bold;
}

/* Search Field Styles */
.search-field {
    -fx-background-color: #FFFFFF;
    -fx-border-color: #03045F;
    -fx-border-radius: 5;
    -fx-pref-width: 300;
    -fx-padding: 10;
    -fx-font-size: 14px;
}

.search-field:focused {
    -fx-border-color: #0078D7;
}

/* Add Button Styles */
.add-button {
    -fx-background-color: #03045F;
    -fx-text-fill: #FFFFFF;
    -fx-font-weight: bold;
    -fx-padding: 10 20;
    -fx-border-radius: 5;
    -fx-cursor: hand;
}

.add-button:hover {
    -fx-background-color: #0078D7;
}

/* Table Styles */
.styled-table {
    -fx-background-color: #FFFFFF;
    -fx-border-color: #E0E0E0;
    -fx-border-radius: 5;
    -fx-padding: 10;
}

.styled-table .column-header {
    -fx-background-color: #03045F;
    -fx-text-fill: #FFFFFF;
    -fx-font-weight: bold;
    -fx-font-size: 14px;
}

.styled-table .column-header-background {
    -fx-background-color: #03045F;
}

.styled-table .table-row-cell {
    -fx-background-color: #FFFFFF;
    -fx-border-color: #E0E0E0;
    -fx-border-width: 0 0 1 0;
}

.styled-table .table-row-cell:odd {
    -fx-background-color: #F9F9F9;
}

.styled-table .table-row-cell:selected {
    -fx-background-color: #0078D7;
    -fx-text-fill: #FFFFFF;
}

.styled-table .table-cell {
    -fx-padding: 10;
    -fx-alignment: CENTER_LEFT;
}

/* Avatar Column Styles */
.image-column {
    -fx-alignment: CENTER;
}

/* Update and Delete Button Styles */
.update-button, .delete-button {
    -fx-background-color: #03045F;
    -fx-text-fill: #FFFFFF;
    -fx-font-weight: bold;
    -fx-padding: 5 10;
    -fx-border-radius: 3;
    -fx-cursor: hand;
}

.update-button:hover {
    -fx-background-color: #0078D7;
}

.delete-button:hover {
    -fx-background-color: #D32F2F;
}
/* General Styles */
.root {
    -fx-font-family: 'Segoe UI', sans-serif;
    -fx-font-size: 14px;
    -fx-background-color: #F0F4F8;
}

/* Header Styles */
.header-label {
    -fx-text-fill: #FFFFFF;
    -fx-font-size: 36px;
    -fx-font-weight: bold;
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 10, 0.5, 0, 1);
    -fx-padding: 20;
    -fx-background-color: #03045F;
    -fx-background-radius: 0 0 10 10;
}

/* Search Field Styles */
.search-field {
    -fx-background-color: #FFFFFF;
    -fx-border-color: #03045F;
    -fx-border-radius: 20;
    -fx-pref-width: 300;
    -fx-padding: 10;
    -fx-font-size: 14px;
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 5, 0, 0, 2);
    -fx-transition: all 0.3s ease;
}

.search-field:focused {
    -fx-border-color: #0078D7;
    -fx-effect: dropshadow(gaussian, rgba(0, 120, 215, 0.3), 10, 0.5, 0, 1);
}

/* Add Button Styles */
.add-button {
    -fx-background-color: #03045F;
    -fx-text-fill: #FFFFFF;
    -fx-font-weight: bold;
    -fx-padding: 10 20;
    -fx-border-radius: 20;
    -fx-cursor: hand;
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0, 0, 2);
    -fx-transition: all 0.3s ease;
}

.add-button:hover {
    -fx-background-color: #0078D7;
    -fx-effect: dropshadow(gaussian, rgba(0, 120, 215, 0.4), 10, 0.5, 0, 3);
}

/* Table Styles */
.styled-table {
    -fx-background-color: #FFFFFF;
    -fx-border-color: #E0E0E0;
    -fx-border-radius: 10;
    -fx-padding: 10;
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 10, 0.5, 0, 2);
}
/* Root styling */
.root {
    -fx-font-family: "Arial";
    -fx-background-color: #ffffff; /* White background */
}

/* GridPane Styling */
.grid-pane {
    -fx-background-color: #ffffff; /* White background */
    -fx-padding: 20;
    -fx-alignment: center;
    -fx-spacing: 10; /* Consistent spacing between elements */
}

/* Labels */
.label {
    -fx-font-size: 14px;
    -fx-text-fill: #333333; /* Dark gray text */
}

/* Text Fields */
.text-field {
    -fx-background-color: #f5f5f5; /* Light gray background */
    -fx-border-color: #cccccc; /* Light gray border */
    -fx-border-width: 1px;
    -fx-border-radius: 5px; /* Rounded corners */
    -fx-padding: 10px;
    -fx-pref-width: 250px; /* Medium size */
    -fx-font-size: 14px;
    -fx-text-fill: #333333; /* Dark gray text */
    -fx-prompt-text-fill: #999999; /* Light gray placeholder text */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 5, 0.5, 0, 2); /* Subtle shadow */
}

.text-field:focused {
    -fx-border-color: #4a90e2; /* Blue border when focused */
    -fx-effect: dropshadow(gaussian, rgba(74, 144, 226, 0.3), 10, 0.5, 0, 5); /* Glow effect */
}

/* Title Label */
.title-label {
    -fx-font-size: 24px;
    -fx-font-weight: bold;
    -fx-text-fill: #333333; /* Dark gray for contrast */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 2, 0.5, 0, 1); /* Subtle shadow */
}

/* Buttons */
.button {
    -fx-font-size: 14px;
    -fx-padding: 10px 20px;
    -fx-border-radius: 5px;
    -fx-cursor: hand;
}

.cancel-button {
    -fx-background-color: #cccccc; /* Light gray background */
    -fx-text-fill: #333333; /* Dark gray text */
}

.cancel-button:hover {
    -fx-background-color: #bbbbbb; /* Darker gray on hover */
}

.update-button {
    -fx-background-color: #4a90e2; /* Blue background */
    -fx-text-fill: white; /* White text */
}

.update-button:hover {
    -fx-background-color: #357ABD; /* Darker blue on hover */
}
styled-table .column-header {
    -fx-background-color: #FFFFFF; /* White background */
    -fx-text-fill: #FFFFFFF; /* Dark blue text */
    -fx-font-weight: bold;
    -fx-font-size: 14px;
    -fx-border-radius: 5;
    -fx-border-color: #E0E0E0; /* Light gray border */
    -fx-border-width: 0 0 1 0; /* Bottom border only */
}

.styled-table .column-header:hover {
    -fx-background-color: #F0F4F8; /* Light gray background on hover */
    -fx-text-fill: #0078D7; /* Blue text on hover */
}

.styled-table .column-header-background {
    -fx-background-color: transparent;
}

.styled-table .table-row-cell {
    -fx-background-color: #FFFFFF;
    -fx-border-color: #E0E0E0;
    -fx-border-width: 0 0 1 0;
    -fx-transition: all 0.3s ease;
}

.styled-table .table-row-cell:odd {
    -fx-background-color: #FFFFFF;
}

.styled-table .table-row-cell:hover {
    -fx-background-color: #E3F2FD;
    -fx-cursor: hand;
}

.styled-table .table-row-cell:selected {
    -fx-background-color: #0078D7;
    -fx-text-fill: #FFFFFF;
}

.styled-table .table-cell {
    -fx-padding: 10;
    -fx-alignment: CENTER_LEFT;
    -fx-text-fill: #333333;
}

/* Avatar Column Styles */
.image-column {
    -fx-alignment: CENTER;
}

/* Update and Delete Button Styles */
.update-button, .delete-button {
    -fx-background-color: #03045F;
    -fx-text-fill: #FFFFFF;
    -fx-font-weight: bold;
    -fx-padding: 5 10;
    -fx-border-radius: 10;
    -fx-cursor: hand;
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0, 0, 2);
    -fx-transition: all 0.3s ease;
}

.update-button:hover {
    -fx-background-color: #0078D7;
    -fx-effect: dropshadow(gaussian, rgba(0, 120, 215, 0.4), 10, 0.5, 0, 3);
}

.delete-button:hover {
    -fx-background-color: #D32F2F;
    -fx-effect: dropshadow(gaussian, rgba(211, 47, 47, 0.4), 10, 0.5, 0, 3);
}

/* Scrollbar Styles */
.styled-table .scroll-bar:horizontal,
.styled-table .scroll-bar:vertical {
    -fx-background-color: transparent;
}

.styled-table .scroll-bar:horizontal .thumb,
.styled-table .scroll-bar:vertical .thumb {
    -fx-background-color: #03045F;
    -fx-background-radius: 10;
}

.styled-table .scroll-bar:horizontal .thumb:hover,
.styled-table .scroll-bar:vertical .thumb:hover {
    -fx-background-color: #0078D7;
}

/* Table Header Hover Effect */


/* Right Pane Styling */
.right-pane {
    -fx-background-color: #ffffff; /* White background */
    -fx-alignment: center;
    -fx-padding: 40;
}

/* Sign-In Title Label */
.signin-title-label {
    -fx-font-size: 28px;
    -fx-font-weight: bold;
    -fx-text-fill: #333333; /* Dark gray for contrast */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 2, 0.5, 0, 1); /* Subtle shadow */
}

/* Input Fields (Email and Password) */
.input-field {
    -fx-background-color: #f5f5f5; /* Light gray background */
    -fx-border-color: #cccccc; /* Light gray border */
    -fx-border-width: 1px;
    -fx-border-radius: 5px; /* Rounded corners */
    -fx-padding: 10px;
    -fx-pref-width: 250px; /* Medium size */
    -fx-font-size: 14px;
    -fx-text-fill: #333333; /* Dark gray text */
    -fx-prompt-text-fill: #999999; /* Light gray placeholder text */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.1), 5, 0.5, 0, 2); /* Subtle shadow */
}

.input-field:focused {
    -fx-border-color: #4a90e2; /* Blue border when focused */
    -fx-effect: dropshadow(gaussian, rgba(74, 144, 226, 0.3), 10, 0.5, 0, 5); /* Glow effect */
}

/* Sign-In Button */
.signin-button {
    -fx-background-color: linear-gradient(to bottom right, #1e3c72, #2a5298); /* Gradient background */
    -fx-text-fill: white; /* White text */
    -fx-font-size: 16px;
    -fx-padding: 12px 30px; /* Padding for button */
    -fx-border-radius: 5px; /* Rounded corners */
    -fx-cursor: hand; /* Hand cursor on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.2), 5, 0.5, 0, 2); /* Subtle shadow */
}

.signin-button:hover {
    -fx-background-color: linear-gradient(to bottom right, #2a5298, #1e3c72); /* Reverse gradient on hover */
    -fx-effect: dropshadow(gaussian, rgba(0, 0, 0, 0.3), 10, 0.5, 0, 5); /* Larger shadow on hover */
}

/* Link Labels (Forgot Password and Sign Up) */
.link-label {
    -fx-font-size: 14px;
    -fx-text-fill: #4a90e2; /* Blue text */
    -fx-underline: true; /* Underline for link effect */
    -fx-cursor: hand; /* Hand cursor on hover */
}

.link-label:hover {
    -fx-text-fill: #357ABD; /* Darker blue on hover */
    -fx-effect: dropshadow(gaussian, rgba(74, 144, 226, 0.3), 2, 0.5, 0, 1); /* Subtle glow on hover */
}